<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizApp">
        <activity android:name=".ResultActivity"></activity>
        <activity android:name=".QuizQuestionActivity"></activity>
        <!--
        windowSoftInputMode esto para que se resize cuando aparez el teclado en la primera screem
        podemos leer mas en la documentacion de androidEstudio en el siguiente link https://developer.android.com/guide/topics/manifest/activity-element
        -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBarTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>